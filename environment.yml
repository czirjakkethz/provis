$ conda activate my-rdkit-env
(my-rdkit-env) $ conda env export --from-history | tee environment.yml
name: my-rdkit-env
channels:
  - defaults
  - conda-forge
prefix: /home/kczi/anaconda3/envs/my-rdkit-env
